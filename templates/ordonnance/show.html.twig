{% extends 'base.html.twig' %}

{% block title %}Ordonnance{% endblock %}

{% block body %}
    <h1>Ordonnance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordonnance.id }}</td>
            </tr>
            <tr>
                <th>DateEmission</th>
                <td>{{ ordonnance.dateEmission ? ordonnance.dateEmission|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Commentaires</th>
                <td>{{ ordonnance.commentaires }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ordonnance_index') }}">back to list</a>

    <a href="{{ path('app_ordonnance_edit', {'id': ordonnance.id}) }}">edit</a>

    {{ include('ordonnance/_delete_form.html.twig') }}
{% endblock %}
