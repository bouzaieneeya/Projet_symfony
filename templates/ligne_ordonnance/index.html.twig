{% extends 'base.html.twig' %}

{% block title %}LigneOrdonnance index{% endblock %}

{% block body %}
    <h1>LigneOrdonnance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>Posologie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne_ordonnance in ligne_ordonnances %}
            <tr>
                <td>{{ ligne_ordonnance.id }}</td>
                <td>{{ ligne_ordonnance.quantite }}</td>
                <td>{{ ligne_ordonnance.posologie }}</td>
                <td>
                    <a href="{{ path('app_ligne_ordonnance_show', {'id': ligne_ordonnance.id}) }}">show</a>
                    <a href="{{ path('app_ligne_ordonnance_edit', {'id': ligne_ordonnance.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ligne_ordonnance_new') }}">Create new</a>
{% endblock %}
