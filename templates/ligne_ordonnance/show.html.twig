{% extends 'base.html.twig' %}

{% block title %}LigneOrdonnance{% endblock %}

{% block body %}
    <h1>LigneOrdonnance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_ordonnance.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ligne_ordonnance.quantite }}</td>
            </tr>
            <tr>
                <th>Posologie</th>
                <td>{{ ligne_ordonnance.posologie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne_ordonnance_index') }}">back to list</a>

    <a href="{{ path('app_ligne_ordonnance_edit', {'id': ligne_ordonnance.id}) }}">edit</a>

    {{ include('ligne_ordonnance/_delete_form.html.twig') }}
{% endblock %}
