<form method="post" action="{{ path('app_ligne_ordonnance_delete', {'id': ligne_ordonnance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_ordonnance.id) }}">
    <button class="btn">Delete</button>
</form>
